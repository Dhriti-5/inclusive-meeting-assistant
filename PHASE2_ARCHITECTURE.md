# Phase 2 Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           FRONTEND (React)                               │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  Login Page  │  Register Page  │  Meeting Page  │  History Page  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                   │                                      │
│                     [JWT Token in localStorage]                         │
│                                   │                                      │
└───────────────────────────────────┼──────────────────────────────────────┘
                                    │
                    HTTP Requests with Bearer Token
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          BACKEND (FastAPI)                               │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                     Auth Middleware                                │ │
│  │  ┌──────────────────────────────────────────────────────────────┐│ │
│  │  │  1. Extract JWT token from Authorization header             ││ │
│  │  │  2. Validate token signature and expiration                 ││ │
│  │  │  3. Extract user email from token                           ││ │
│  │  │  4. Proceed to endpoint or return 401 Unauthorized          ││ │
│  │  └──────────────────────────────────────────────────────────────┘│ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                    API Endpoints                                   │ │
│  │                                                                    │ │
│  │  Authentication (Public):                                         │ │
│  │    POST /api/auth/register  → Create user account                │ │
│  │    POST /api/auth/login     → Get JWT token                      │ │
│  │                                                                    │ │
│  │  User Info (Protected):                                           │ │
│  │    GET  /api/auth/me        → Get current user                   │ │
│  │                                                                    │ │
│  │  Meetings (Protected):                                            │ │
│  │    POST /api/meetings/join           → Create meeting            │ │
│  │    GET  /api/meetings/{id}/status    → Get status                │ │
│  │    GET  /api/meetings/history        → Get user's meetings       │ │
│  │    POST /api/meetings/{id}/upload    → Upload audio              │ │
│  │    GET  /api/meetings/{id}/report    → Get full report           │ │
│  │    GET  /api/meetings/{id}/pdf       → Download PDF              │ │
│  │    POST /api/meetings/{id}/email     → Send via email            │ │
│  │                                                                    │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                   Security Components                              │ │
│  │                                                                    │ │
│  │  Password Hashing (passlib + bcrypt):                            │ │
│  │    password → bcrypt_hash → stored in database                   │ │
│  │                                                                    │ │
│  │  JWT Token Generation (python-jose):                             │ │
│  │    user_email + secret_key → signed_token                        │ │
│  │    Token contains: {sub: email, exp: timestamp}                  │ │
│  │                                                                    │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                          │
└──────────────────────────────────┬───────────────────────────────────────┘
                                   │
                          MongoDB Connection
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    MongoDB Database (Motor - Async)                      │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │  Database: inclusive_meeting_assistant                            │ │
│  │                                                                    │ │
│  │  ┌─────────────────────────────────────────────────────────────┐ │ │
│  │  │  Collection: users                                          │ │ │
│  │  │  ┌───────────────────────────────────────────────────────┐ │ │ │
│  │  │  │  {                                                     │ │ │ │
│  │  │  │    "_id": ObjectId("..."),                            │ │ │ │
│  │  │  │    "email": "student@college.edu",                    │ │ │ │
│  │  │  │    "password_hash": "$2b$12$...",                     │ │ │ │
│  │  │  │    "name": "Student Name",                            │ │ │ │
│  │  │  │    "created_at": ISODate("2025-12-16T...")            │ │ │ │
│  │  │  │  }                                                     │ │ │ │
│  │  │  └───────────────────────────────────────────────────────┘ │ │ │
│  │  └─────────────────────────────────────────────────────────────┘ │ │
│  │                                                                    │ │
│  │  ┌─────────────────────────────────────────────────────────────┐ │ │
│  │  │  Collection: meetings                                       │ │ │
│  │  │  ┌───────────────────────────────────────────────────────┐ │ │ │
│  │  │  │  {                                                     │ │ │ │
│  │  │  │    "_id": "mtg_abc123",                               │ │ │ │
│  │  │  │    "user_id": "user_id_ref",  ← Links to user        │ │ │ │
│  │  │  │    "participant_name": "Team Meeting",                │ │ │ │
│  │  │  │    "status": "completed",                             │ │ │ │
│  │  │  │    "transcript": [                                    │ │ │ │
│  │  │  │      {"speaker": "SPEAKER_01", "text": "...", ...}   │ │ │ │
│  │  │  │    ],                                                 │ │ │ │
│  │  │  │    "summary": "Meeting summary...",                   │ │ │ │
│  │  │  │    "action_items": ["Item 1", "Item 2"],             │ │ │ │
│  │  │  │    "created_at": ISODate("..."),                      │ │ │ │
│  │  │  │    "ended_at": ISODate("...")                         │ │ │ │
│  │  │  │  }                                                     │ │ │ │
│  │  │  └───────────────────────────────────────────────────────┘ │ │ │
│  │  └─────────────────────────────────────────────────────────────┘ │ │
│  │                                                                    │ │
│  └───────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

## Authentication Flow

```
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Register
     │    POST /api/auth/register
     │    {email, password, name}
     │
     ▼
┌────────────────┐         ┌──────────────┐
│  Backend       │────────▶│  MongoDB     │
│  hash password │         │  save user   │
└────────────────┘         └──────────────┘
     │
     │ 2. Returns user info (no password)
     │
     ▼
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 3. Login
     │    POST /api/auth/login
     │    {username: email, password}
     │
     ▼
┌────────────────┐         ┌──────────────┐
│  Backend       │────────▶│  MongoDB     │
│  verify pass   │         │  find user   │
│  create JWT    │         └──────────────┘
└────────────────┘
     │
     │ 4. Returns JWT token
     │    {access_token: "eyJhbGc...", token_type: "bearer"}
     │
     ▼
┌──────────────────┐
│  Frontend        │
│  Store token in  │
│  localStorage    │
└──────────────────┘
     │
     │ 5. Protected API calls
     │    GET /api/meetings/history
     │    Headers: {Authorization: "Bearer eyJhbGc..."}
     │
     ▼
┌────────────────┐
│  Backend       │
│  1. Extract    │
│     token      │
│  2. Verify     │
│     signature  │
│  3. Check      │
│     expiration │
│  4. Extract    │
│     user email │
└────────────────┘
     │
     │ 6. Query user's data only
     │
     ▼
┌──────────────┐
│  MongoDB     │
│  Find where  │
│  user_id =   │
│  current_id  │
└──────────────┘
     │
     │ 7. Returns filtered data
     │
     ▼
┌──────────┐
│  User    │
└──────────┘
```

## Data Flow: Creating a Meeting

```
Frontend                Backend                    MongoDB
   │                       │                          │
   │  1. Click "Join"     │                          │
   │  ──────────────────▶ │                          │
   │  POST /meetings/join │                          │
   │  {name: "Meeting"}   │                          │
   │  Bearer token        │                          │
   │                      │                          │
   │                      │ 2. Validate token       │
   │                      │    Extract user_email   │
   │                      │                          │
   │                      │ 3. Find user            │
   │                      │ ──────────────────────▶ │
   │                      │                          │
   │                      │ 4. Return user doc      │
   │                      │ ◀────────────────────── │
   │                      │                          │
   │                      │ 5. Create meeting doc   │
   │                      │    {_id, user_id, ...}  │
   │                      │ ──────────────────────▶ │
   │                      │                          │
   │                      │ 6. Insert successful    │
   │                      │ ◀────────────────────── │
   │                      │                          │
   │ 7. Return meeting    │                          │
   │ ◀────────────────── │                          │
   │  {meeting_id: "abc"}│                          │
   │                      │                          │
   │  8. Redirect to      │                          │
   │     meeting page     │                          │
   │                      │                          │
```

## Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 1: Network Security                                   │
│  - HTTPS in production                                       │
│  - CORS configured for specific origins                      │
│  - Rate limiting (TODO)                                      │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 2: Authentication                                     │
│  - JWT token required for all protected routes               │
│  - Token validated on every request                          │
│  - 401 Unauthorized if missing/invalid                       │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 3: Password Security                                  │
│  - Bcrypt hashing with automatic salt                        │
│  - Never stored in plaintext                                 │
│  - Computationally expensive to crack                        │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 4: Data Isolation                                     │
│  - Queries filtered by user_id                               │
│  - Users can only access their own data                      │
│  - Meeting ownership verified                                │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 5: Database Security                                  │
│  - MongoDB authentication (production)                       │
│  - Connection string in environment variables                │
│  - Indexes for performance and integrity                     │
└─────────────────────────────────────────────────────────────┘
```

## Token Structure

```
JWT Token Format:
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ1c2VyQGV4YW1wbGUuY29tIiwiZXhwIjoxNzM5NzA3MjAwfQ.signature
│                                      │                                                                              │
│         HEADER                       │                    PAYLOAD                                                   │  SIGNATURE
│  (Algorithm & Type)                  │            (User data & Expiration)                                          │  (Verification)
│                                      │                                                                              │
│  {                                   │  {                                                                           │  HMACSHA256(
│    "alg": "HS256",                   │    "sub": "user@example.com",  ← User email                                 │    base64(header) + "." +
│    "typ": "JWT"                      │    "exp": 1739707200           ← Expiration timestamp                       │    base64(payload),
│  }                                   │  }                                                                           │    secret_key
│                                      │                                                                              │  )
│                                      │                                                                              │
└──── Base64 Encoded ─────────────────└──────────── Base64 Encoded ────────────────────────────────────────────────└─── Signed ────
```

## File Structure

```
inclusive-meeting-assistant/
│
├── backend/
│   ├── main.py              ← Main FastAPI app (+ auth endpoints, protected routes)
│   ├── database.py          ← NEW: MongoDB connection & helpers
│   ├── auth.py              ← NEW: JWT & password utilities
│   ├── models.py            ← NEW: Pydantic models for DB schemas
│   ├── pipeline_runner.py
│   └── speaker_diarization.py
│
├── .env                     ← Configuration
│   ├── MONGODB_URL          ← NEW: mongodb://localhost:27017
│   ├── DATABASE_NAME        ← NEW: inclusive_meeting_assistant
│   ├── JWT_SECRET_KEY       ← NEW: Secret for signing tokens
│   ├── HF_TOKEN
│   ├── SENDER_EMAIL
│   └── APP_PASSWORD
│
├── requirements.txt         ← Dependencies
│   ├── motor                ← NEW: Async MongoDB driver
│   ├── python-jose          ← NEW: JWT library
│   ├── passlib              ← NEW: Password hashing
│   ├── python-multipart     ← NEW: Form data
│   └── email-validator      ← NEW: Email validation
│
├── test_phase2_auth_mongodb.py  ← NEW: Comprehensive test script
│
├── PHASE2_IMPLEMENTATION.md     ← NEW: Full technical docs
├── QUICKSTART_PHASE2.md         ← NEW: Quick setup guide
├── PHASE2_SUMMARY.md            ← NEW: Executive summary
└── PHASE2_ARCHITECTURE.md       ← NEW: This file (visual diagrams)
```

## Comparison: Before vs After

```
┌──────────────────────────────────┬──────────────────────────────────┐
│         PHASE 1 (Before)         │         PHASE 2 (After)          │
├──────────────────────────────────┼──────────────────────────────────┤
│  Storage:                        │  Storage:                        │
│    ❌ In-memory dict             │    ✅ MongoDB (persistent)       │
│    ❌ Lost on restart            │    ✅ Survives restarts          │
│    ❌ Not scalable               │    ✅ Scales to millions         │
│                                  │                                  │
│  Authentication:                 │  Authentication:                 │
│    ❌ None                       │    ✅ JWT tokens                 │
│    ❌ Anyone can access          │    ✅ User accounts              │
│    ❌ No security                │    ✅ Bcrypt passwords           │
│                                  │                                  │
│  Data Access:                    │  Data Access:                    │
│    ❌ Global meetings_db         │    ✅ Per-user isolation         │
│    ❌ No ownership               │    ✅ Ownership model            │
│    ❌ Privacy issues             │    ✅ Secure & private           │
│                                  │                                  │
│  Production Ready:               │  Production Ready:               │
│    ❌ Demo only                  │    ✅ Professional grade         │
│    ❌ Can't deploy               │    ✅ Deploy-ready               │
│    ❌ Not secure                 │    ✅ Industry-standard          │
└──────────────────────────────────┴──────────────────────────────────┘
```

---

**This architecture provides a solid foundation for a production-ready meeting assistant application!** 🎉
